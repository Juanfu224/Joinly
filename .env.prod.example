# =============================================================================
# JOINLY - VARIABLES DE ENTORNO DE PRODUCCIÓN
# =============================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo: cp .env.prod.example .env.prod
# 2. Generar TODAS las claves secretas con los comandos indicados
# 3. Configurar el dominio y email
# 4. NO subir .env.prod a Git (está en .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURACIÓN GENERAL
# -----------------------------------------------------------------------------
# Dominio de la aplicación (sin https://)
DOMAIN=joinly.example.com

# Email para notificaciones de Let's Encrypt
LETSENCRYPT_EMAIL=admin@example.com

# Versión de la aplicación (usado para tags de imágenes Docker)
APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# BASE DE DATOS - MySQL
# -----------------------------------------------------------------------------
# Generar contraseña segura: openssl rand -base64 32
MYSQL_ROOT_PASSWORD=GENERAR_CON_OPENSSL_RAND_BASE64_32
MYSQL_DATABASE=bbdd_joinly
MYSQL_USER=joinly_user
MYSQL_PASSWORD=GENERAR_CON_OPENSSL_RAND_BASE64_32

# -----------------------------------------------------------------------------
# BACKEND - Spring Boot
# -----------------------------------------------------------------------------
# Credenciales de base de datos para Spring Boot
# (usar los mismos valores que MYSQL_USER y MYSQL_PASSWORD)
DB_USERNAME=joinly_user
DB_PASSWORD=MISMO_QUE_MYSQL_PASSWORD

# -----------------------------------------------------------------------------
# JWT - JSON Web Tokens
# -----------------------------------------------------------------------------
# CRÍTICO: Generar clave única y segura
# Comando: openssl rand -base64 64
JWT_SECRET_KEY=GENERAR_CON_OPENSSL_RAND_BASE64_64

# Expiración de tokens (en milisegundos)
# Access token: 1 hora = 3600000
# Refresh token: 30 días = 2592000000
JWT_ACCESS_TOKEN_EXPIRATION=3600000
JWT_REFRESH_TOKEN_EXPIRATION=2592000000

# -----------------------------------------------------------------------------
# ENCRIPTACIÓN AES-256
# -----------------------------------------------------------------------------
# CRÍTICO: Generar clave única y segura
# Comando: openssl rand -base64 32
ENCRYPTION_KEY=GENERAR_CON_OPENSSL_RAND_BASE64_32

# =============================================================================
# CHECKLIST DE SEGURIDAD ANTES DEL DEPLOY:
# =============================================================================
# [ ] Todas las contraseñas generadas con openssl (no valores por defecto)
# [ ] JWT_SECRET_KEY generado con openssl rand -base64 64
# [ ] ENCRYPTION_KEY generado con openssl rand -base64 32
# [ ] DOMAIN configurado correctamente
# [ ] LETSENCRYPT_EMAIL es un email válido
# [ ] Archivo NO subido a Git
# [ ] Backups de las claves en lugar seguro
# =============================================================================
