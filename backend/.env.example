# Joinly Backend - Variables de Entorno
# ⚠️ IMPORTANTE: Este archivo NO contiene valores reales
# 1. Copiar: cp .env.example .env
# 2. Rellenar con valores seguros generados
# 3. NUNCA commitear el archivo .env

# Base de Datos (REQUERIDO)
DB_URL=jdbc:mysql://localhost:3306/bbdd_joinly
DB_USERNAME=root
DB_PASSWORD=

# JWT (REQUERIDO) - Generar: openssl rand -base64 64
JWT_SECRET_KEY=

# Encriptación AES-256 (REQUERIDO) - Generar: openssl rand -base64 32
ENCRYPTION_KEY=

# CORS Producción (REQUERIDO en perfil prod)
CORS_ALLOWED_ORIGIN=https://joinly.app

# Docker MySQL (opcional, para docker-compose.yml)
MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=bbdd_joinly
