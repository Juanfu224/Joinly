spring.application.name=joinly

# Configuraci�n del puerto del servidor
server.port=8080

# Configuracion de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/bbdd_joinly
spring.datasource.username=root
spring.datasource.password=*Juanfu224*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Configuracion de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Deshabilitar verificación de templates (API REST sin vistas)
spring.thymeleaf.check-template-location=false

# Silenciar warning informativo de Spring Security (configuración intencional)
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

# ========================================
# Configuración JWT
# ========================================
# Clave secreta para firmar tokens JWT (Base64 encoded, mínimo 256 bits)
# IMPORTANTE: Cambiar en producción y mantener en secreto
jwt.secret-key=TlVFU1RST19TRUNSRVRPXygyMDI1KV9QQVJBX0pPSU5MWV9KVVU5WjgyVFhPODM4TDgzT0VNTQ==
# Tiempo de expiración del access token (en milisegundos) - 1 hora
jwt.access-token-expiration=3600000
# Tiempo de expiración del refresh token (en milisegundos) - 30 días
jwt.refresh-token-expiration=2592000000

# ========================================
# Configuración CORS
# ========================================
# Orígenes permitidos (formato lista indexada)
cors.allowed-origins[0]=http://localhost:4200
cors.allowed-origins[1]=http://localhost:3000
# Métodos HTTP permitidos
cors.allowed-methods[0]=GET
cors.allowed-methods[1]=POST
cors.allowed-methods[2]=PUT
cors.allowed-methods[3]=PATCH
cors.allowed-methods[4]=DELETE
cors.allowed-methods[5]=OPTIONS
# Headers permitidos
cors.allowed-headers[0]=Authorization
cors.allowed-headers[1]=Content-Type
cors.allowed-headers[2]=X-Requested-With
cors.allowed-headers[3]=Accept
cors.allowed-headers[4]=Origin
# Headers expuestos al frontend
cors.exposed-headers[0]=Authorization
# Tiempo de caché para preflight (segundos)
cors.max-age=3600