# =============================================================================
# ARCHIVO DE EJEMPLO DE VARIABLES DE ENTORNO
# =============================================================================
# INSTRUCCIONES:
# 1. Copiar este archivo y renombrarlo a '.env' (sin .example)
# 2. Completar los valores reales
# 3. NO subir el archivo .env a Git (ya está en .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE MYSQL (Docker Compose)
# -----------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=bbdd_joinly
MYSQL_USER=joinly_user
MYSQL_PASSWORD=your_password
MYSQL_PORT=3306

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS (Spring Boot)
# -----------------------------------------------------------------------------
DB_URL=jdbc:mysql://localhost:3306/bbdd_joinly
DB_USERNAME=root
DB_PASSWORD=your_root_password

# -----------------------------------------------------------------------------
# CONFIGURACIÓN JWT (JSON Web Tokens)
# -----------------------------------------------------------------------------
# Generar clave segura con: openssl rand -base64 64
JWT_SECRET_KEY=GENERAR_UNA_CLAVE_ALEATORIA_SEGURA_CON_OPENSSL
JWT_ACCESS_TOKEN_EXPIRATION=3600000
JWT_REFRESH_TOKEN_EXPIRATION=2592000000

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE ENCRIPTACIÓN AES-256
# -----------------------------------------------------------------------------
# Generar clave segura con: openssl rand -base64 32
ENCRYPTION_KEY=GENERAR_UNA_CLAVE_AES_256_CON_OPENSSL

# =============================================================================
# NOTAS DE SEGURIDAD:
# =============================================================================
# - NUNCA compartir este archivo con las claves reales
# - NUNCA subir el archivo .env a Git
# - Cambiar todas las claves en producción
# - Las claves deben ser aleatorias y únicas para cada entorno
# - En producción, usar variables de entorno del sistema o servicio de secrets
# =============================================================================
